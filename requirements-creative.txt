# Creative / video pipeline Python deps — installed by scripts/setup-mac-creative-pipeline.sh
# Pin to v2 — MoviePy v1 is unmaintained and has breaking API differences (see SETUP-MOVIEPY.md).
moviepy>=2.0
# Lightweight, fast TTS for HyperFrames narration (see KOKORO-SETUP.md).
kokoro-tts
