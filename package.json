{
  "name": "jamie-wigg",
  "private": true,
  "description": "Workspace root. The Remotion video project lives in video/. This root package.json exists only to host repo-level dev tools (e.g. claude-playwright MCP server).",
  "scripts": {
    "generate-with-gemini": "node scripts/gemini-nano-banana-generator.mjs",
    "generate-with-gemini:social": "node scripts/gemini-nano-banana-generator.mjs social-media-post",
    "generate-with-gemini:product": "node scripts/gemini-nano-banana-generator.mjs product-marketing"
  },
  "devDependencies": {
    "claude-playwright": "^0.1.4"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
