{
  "name": "@ecosystem/domain-data",
  "version": "0.1.0",
  "private": true,
  "description": "Data science domain (Jupyter, R, SQL, pandas, etc.)",
  "scripts": {
    "dev": "jupyter notebook",
    "build": "echo 'Data science projects build differently'",
    "test": "pytest"
  }
}
