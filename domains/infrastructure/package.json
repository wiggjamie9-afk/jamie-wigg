{
  "name": "@ecosystem/domain-infrastructure",
  "version": "0.1.0",
  "private": true,
  "description": "Infrastructure domain (Docker, Kubernetes, Terraform, CI/CD)",
  "scripts": {
    "dev": "echo 'Infrastructure projects use different tooling'",
    "build": "docker build .",
    "test": "terraform validate"
  }
}
