node_modules/
.remotion/
*/node_modules/
*/.remotion/
graphify-out/cache/
*.pack.old
*/work-*/

# Creative-stack MCP outputs (regenerated, not source)
creative-out/
.claude/mcp/creative-stack/node_modules/

# RHYTHMIX Studio engine outputs (per-render scenes + logs, regenerated)
rhythmix-out/

# Stray test artifacts from rhythmix-studio CLI tests invoked from the
# repo root instead of inside rhythmix-studio/ — regenerable fixtures.
/test-input/
/test/

# Local Claude config — contains API tokens, never commit
.claude/settings.local.json

# Sub-agent transient worktrees (created and reclaimed by the Agent SDK)
.claude/worktrees/

# Local secrets for MCP servers (Higgsfield, etc.) — never commit
.env
*/.env
vendor/
