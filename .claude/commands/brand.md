---
description: Run a RHYTHMIX strategic brand-positioning (Purple Cow) prompt
---

Run a strategic RHYTHMIX brand-positioning prompt.

**Argument:** `$ARGUMENTS` — name of the prompt to run. Accepts:

- `symbol` — pressure-test or design the signature visual symbol
- `slogan` — generate 5 candidate slogans (current incumbent: "Turn any idea into music")
- `surprise` — find the counterintuitive industry truth RHYTHMIX is built on
- `salient` — sharpen the single salient idea (run this first)
- `story` — build the 3-minute signature brand story

**Steps:**

1. If `$ARGUMENTS` is empty, list the five options above. Recommend running `salient` and `slogan` first. Stop.
2. Read `prompts/brand-positioning.md` and find the matching numbered section.
3. Read `landing.html` for the existing positioning, hero, and proof points so the strategic answer doesn't drift from what's already on the page.
4. Read `DESIGN.md` for brand voice and competitor set.
5. Execute the prompt verbatim. Don't paraphrase it first — run it and return the output in the format the prompt asks for.
