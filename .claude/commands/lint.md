---
description: Run eslint + tsc on the video/ Remotion subproject
argument-hint: (no args)
---

Run the Remotion lint check from `video/`.

```bash
cd video && npm run lint
```

If it fails, report the failures verbatim with file:line references and suggest the smallest fix per failure. Do NOT auto-edit files unless I ask.
If it passes, report `READY` and stop.
