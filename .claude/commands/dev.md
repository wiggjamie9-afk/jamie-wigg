---
description: Launch Remotion Studio in the background
---

Start the Remotion Studio dev server in the background:

```bash
cd /home/user/jamie-wigg/video && npm run dev
```

Run it with `run_in_background: true` so the terminal stays free. Report the local URL once it's ready.
