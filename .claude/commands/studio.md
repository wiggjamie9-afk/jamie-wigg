---
description: Start the Remotion studio in the background
allowed-tools: Bash
---

Start the studio so I can preview compositions:

```
cd video && npm run dev
```

Run it in the background and report the URL it printed (default `http://localhost:3000`).
