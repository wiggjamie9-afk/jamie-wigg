{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // \"\"'); case \"$f\" in */graphify-out/*|*/text*.txt) echo \"Blocked: \\\"$f\\\" is protected (CLAUDE.md says do not edit text*.txt or graphify-out/).\" >&2; exit 2;; esac"
          }
        ]
      }
    ]
  }
}
