# Capacitor outputs
node_modules/
www/

# Native projects (generated by `npx cap add ios` / `cap add android`).
# Capacitor's official guidance is to commit these, but in this repo we
# treat them as build artifacts so the repo stays cross-platform-friendly.
# If you start customising native code, remove the entries below.
ios/
android/

# Capacitor temp / cache
.DS_Store
.capacitor/

# Secrets
.env
.env.local
GoogleService-Info.plist
google-services.json
