{
  "name": "starlightmix-herd-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker for HerdCheck co-op aggregation (opt-in sync + staff read-only aggregate over D1).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "echo \"no tests yet — verify with wrangler dev + curl\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
