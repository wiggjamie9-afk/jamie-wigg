{
  "name": "starlightmix-replicate-proxy-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless Cloudflare Worker that proxies browser calls to api.replicate.com to work around missing CORS headers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "echo \"no tests yet — verify with wrangler dev + curl\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
