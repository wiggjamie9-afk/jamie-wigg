# General-purpose Python tooling installed alongside the audio toolkit.
# Not audio-specific — split out so requirements-audio.txt stays focused.
#
# Reinstall in a fresh container with:  pip install -r requirements-tools.txt

# Keon's algorithms / data structures reference library
algorithms==1.0.1

# Faster-whisper — word-level Whisper transcription, CPU/GPU
# (Substitute for whisperx which couldn't build wheels on this container's
# Debian-patched setuptools — pyannote deps julius/antlr4 broke.)
faster-whisper

# yt-dlp — download from YouTube / 1700+ sites
yt-dlp

# piper-tts — local neural TTS (installed but unused because the model voices
# live on huggingface.co which is blocked from this container; mbrola via
# apt was used instead for the v1 RHYTHMIX narrations)
piper-tts
