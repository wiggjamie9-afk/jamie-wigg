# General-purpose Python tooling installed alongside the audio toolkit.
# Not audio-specific — split into its own file so requirements-audio.txt
# stays focused.
#
# Reinstall in a fresh container with:  pip install -r requirements-tools.txt

# Keon's algorithms / data structures reference library — clean, documented
# implementations of sorts, search, graph, tree, DP, etc.
algorithms==1.0.1
