2026-05-20T00:25Z — first deploy after switching Pages source from
gh-pages branch to GitHub Actions. This commit re-fires the
deploy-pages workflow so the existing rhythmix-preview.html (already
on main) finally reaches the published site.
