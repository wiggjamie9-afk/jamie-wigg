{
  "name": "rhythmix-bookreader-pro",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper around BookReader Pro for App Store + Google Play.",
  "scripts": {
    "sync": "npx cap sync",
    "add:ios": "npx cap add ios",
    "add:android": "npx cap add android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "build:android": "cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
