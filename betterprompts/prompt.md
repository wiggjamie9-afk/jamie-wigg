# BetterPrompts — Concept

**Gary's prompt-improvement brain**: A local app that takes a rough prompt and returns a clearer, better-structured version using free LLM APIs (Gemini primary, Groq fallback), with automatic model fallback and session-level configuration.
