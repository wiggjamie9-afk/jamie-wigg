{
  "name": "capacitor-jackson",
  "version": "1.0.0",
  "description": "Jackson's Ecosystem native iOS app",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "cap sync",
    "open:ios": "cap open ios",
    "build:ios": "npm run build && cap sync ios",
    "deploy:ios": "npm run build:ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0",
    "@capacitor/device": "^6.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.3.0"
  }
}
