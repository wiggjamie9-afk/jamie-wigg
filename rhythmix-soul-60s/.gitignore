# Vendor work scratch from hyperframes render
work-*/

# npm install scratch (only needed transiently to vendor three.min.js)
node_modules/

# Unused GLTF asset — left over from an earlier GLTFLoader plan that was
# dropped in favor of a procedural Three.js figure. Sandbox doesn't allow
# rm so it sits here, gitignored.
assets/
