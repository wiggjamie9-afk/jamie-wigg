{
  "name": "resonance-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper around resonance.html for iPhone distribution (TestFlight / App Store).",
  "scripts": {
    "sync:web": "rm -rf www && mkdir -p www && cp ../resonance.html www/index.html && cp ../manifest.json www/ && cp ../sw.js www/ && cp ../icon-192.png www/ && cp ../icon-512.png www/ && npx cap sync ios",
    "add:ios": "npx cap add ios",
    "open:ios": "npx cap open ios",
    "build:ios": "npm run sync:web"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.1",
    "@capacitor/ios": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1",
    "typescript": "^6.0.3"
  }
}
