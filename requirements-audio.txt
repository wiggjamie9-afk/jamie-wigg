# Audio / music / video Python toolkit installed 2026-05-20.
# Reinstall in a fresh container with:  pip install -r requirements-audio.txt
#
# Purpose: reference toolbox for RHYTHMIX audio + video work — analysis,
# mastering, manipulation, metadata, video pipelines, TTS.
# None of these are wired into the current bake / HyperFrames pipeline
# (which uses ffmpeg via shell). Treat this as available, not active.

# Audio analysis (MFCCs, beat tracking, spectrograms, onset detection)
librosa==0.11.0

# Automatic mastering — match target track to a reference master
matchering==2.0.6

# Audio manipulation (slice, concat, crossfade, fade, dB adjust) — pythonic ffmpeg
pydub==0.25.1

# Music library + metadata manager (MusicBrainz, Discogs, ReplayGain, art)
beets==2.11.0

# Lightweight read-only audio metadata reader (no ffmpeg dep)
tinytag==2.2.1

# Multi-threaded video capture / write / streaming framework (OpenCV + FFmpeg under the hood)
vidgear==0.3.5
opencv-python-headless==4.13.0.92  # core dep for vidgear; -headless avoids GTK/X11

# BirdNET acoustic species ID — research toolkit, leaving here for any
# environmental-audio / ambient-sound projects
birdnetlib==0.18.1
tensorflow==2.21.0  # core dep for birdnetlib (CPU-only inference fine)

# Google Translate TTS — installed earlier; endpoint blocked by this
# container's network policy but kept for fresher envs
gTTS==2.5.4
