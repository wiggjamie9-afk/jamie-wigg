{
  "name": "rhythmix-cortex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RHYTHMIX content-factory backbone on Cortex: one entity agent, our generation engines exposed as pathways/tools.",
  "main": "index.mjs",
  "scripts": {
    "start": "CORTEX_ENABLE_REST=true CORTEX_CONFIG_FILE=\"$PWD/node_modules/@aj-archipelago/cortex/config/default.example.json\" node index.mjs",
    "check": "for f in index.mjs pathways/*.js; do node --check \"$f\" || exit 1; done && echo 'all js OK'"
  },
  "dependencies": {
    "@aj-archipelago/cortex": "^1.4.34"
  },
  "engines": {
    "node": ">=22"
  }
}
